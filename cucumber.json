{
  "default": {
    "formatOptions":{
      "snippetInterface": "async-await"
    },
    "paths": ["tests/features"],

    "require": ["tests/step/*.ts"],
    "requireModule":["ts-node/register"]
  }
}
